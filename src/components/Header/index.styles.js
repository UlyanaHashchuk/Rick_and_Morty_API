import styled from 'styled-components'

const Heading = styled.header`
  background-color: #fff;
  padding: 15px;
  font-size: 24px;
`

const ToHome = styled.a`
  color: #202329;

  &:hover {
    border-bottom: 2px solid #b4bfd4;
  }
`

export { Heading, ToHome }
